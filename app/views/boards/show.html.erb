<!-- < % byebug %> -->
<!-- < %  case @game.status %> -->
<!-- < %  when "challenge accepted" %> -->
  <!-- < %= render 'pick_markers' %> -->
<!-- < %  when "game in progress" %> -->
  <%= render 'display_board', :board => @board  %>
<!-- < %  when "game won" %> -->
  <!-- < %= render 'display_board', :board => @board  %> -->
<!-- < % when "game tied" %> -->
  <!-- < %= render 'display_board', :board => @board  %> -->
<!-- < %  else %> -->
 <!-- raise error? -->
<!-- < %  end %> -->